import sys
import pandas as pd

# print(sys.argv) # prints the command line arguments passed to the script
day = sys.argv[1] # first command line argument is the day of the week

# data processing code

print(f"Job finished successfully for day = {day}")